@model IEnumerable<ServiserOnline.Models.Device>

@{
    ViewBag.Title = "Index";
}

<h2>Uredjaji</h2>

<p>
    @Html.ActionLink("Kreiraj novi", "Create", "Devices", new { SelectedProductModel = ViewBag.CreateID }, null)
</p>
@using (Html.BeginForm())
{
    <p>
        Model: @Html.DropDownList("SelectedProductModel", "Svi")
        <input type="submit" value="Filter" />
    </p>
}
<table class="table">
    <tr>
        <th>
            Model
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SerialNumber)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Model.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SerialNumber)
            </td>
            <td>
                @Html.ActionLink("Izmjeni", "Edit", new { id = item.ID }) |
                @Html.ActionLink("Izbriši", "Delete", new { id = item.ID })
            </td>
        </tr>
    }

</table>
@Html.ActionLink("Lista modela", "Index", "ProductModels")